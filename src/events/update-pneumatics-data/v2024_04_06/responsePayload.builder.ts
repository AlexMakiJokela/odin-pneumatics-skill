import {
	buildSchema
} from '@sprucelabs/schema'

const updatePneumaticsDataResponsePayloadBuilder = buildSchema({
    id: 'updatePneumaticsDataResponsePayload',
    fields: {
        
		},
    }
)

export default updatePneumaticsDataResponsePayloadBuilder
